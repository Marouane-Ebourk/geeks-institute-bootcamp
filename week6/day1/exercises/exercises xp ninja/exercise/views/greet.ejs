<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>greet</title>
  <style>
    body {
      background: linear-gradient(135deg, #f9d423 0%, #ff4e50 100%);
      font-family: 'Comic Sans MS', 'Comic Sans', cursive, sans-serif;
      min-height: 100vh;
      margin: 0;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .greet-container {
      background: rgba(255, 255, 255, 0.85);
      border-radius: 20px;
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.2);
      padding: 2.5rem 2rem;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-width: 320px;
      gap: 1.2rem;
      text-align: center;
    }

    h1 {
      color: #ff4e50;
      margin-bottom: 0.5rem;
      font-size: 2.2rem;
      letter-spacing: 1px;
      text-shadow: 1px 2px 8px #f9d42355;
    }

    p {
      font-size: 1.2rem;
      color: #333;
      margin: 0.5rem 0;
    }

    .emoji {
      font-size: 4rem;
      margin-top: 1.2rem;
      filter: drop-shadow(0 2px 8px #ff4e5077);
      animation: pop 0.7s cubic-bezier(.68, -0.55, .27, 1.55);
      display: inline-block;
    }

    @keyframes pop {
      0% {
        transform: scale(0.2) rotate(-20deg);
      }

      70% {
        transform: scale(1.2) rotate(10deg);
      }

      100% {
        transform: scale(1) rotate(0);
      }
    }
  </style>
</head>

<body>
  <div class="greet-container">
    <h1>Welcome to the Greeting Page!</h1>
    <p>Hello, <%= name %>! We're glad to see you here.</p>
    <p>Here's the emoji you've chosen:</p>
    <span class="emoji">
      <%= emoji %>
    </span>
  </div>
</body>

</html>